---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/lib.rs
  - src/error.rs
  - src/constants.rs
  - tests/foundation.rs
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-09

must_haves:
  truths:
    - "`cargo build -p onvif-server` succeeds from a clean state"
    - "`OnvifError::NotImplemented` serializes to a SOAP fault whose detail XML contains `xmlns:ter=\"http://www.onvif.org/ver10/error\"`"
    - "All token constants (PROFILE_TOKEN, VIDEO_SOURCE_TOKEN, PTZ_NODE_TOKEN, PTZ_CONFIG_TOKEN, TRANSLATION_SPACE_FOV) are defined as `pub const &str` and accessible"
  artifacts:
    - path: "Cargo.toml"
      provides: "Crate definition with soap-server path dependency and all required deps"
      contains: "soap-server"
    - path: "src/lib.rs"
      provides: "Crate root with module declarations and public re-exports"
      exports: ["OnvifError", "OnvifServerBuilder", "PROFILE_TOKEN"]
    - path: "src/error.rs"
      provides: "OnvifError enum + into_soap_fault() method"
      contains: "xmlns:ter"
    - path: "src/constants.rs"
      provides: "All crate-level pub const token strings"
      contains: "PROFILE_TOKEN"
    - path: "tests/foundation.rs"
      provides: "Test stubs for INFRA-02 and INFRA-09 (failing stubs for Wave 0)"
  key_links:
    - from: "src/error.rs"
      to: "soap_server::fault::SoapFault"
      via: "into_soap_fault() method"
      pattern: "SoapFault::new"
    - from: "src/lib.rs"
      to: "src/error.rs"
      via: "mod error; pub use error::OnvifError;"
      pattern: "pub use error"
---

<objective>
Create the compilable crate scaffold that all downstream plans depend on: Cargo.toml with the soap-server path dependency and correct library deps, the crate root module structure, OnvifError with ONVIF-namespace SOAP fault mapping, and crate-level token constants.

Purpose: Every other plan in this phase requires a compilable crate. This plan establishes that foundation, including the error type and constants that service traits and builder code reference.
Output: A crate that compiles (`cargo build -p onvif-server`) with correct error types and token constants, plus test stubs that will be filled in by later plans.
</objective>

<execution_context>
@/Users/jhogendorn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jhogendorn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Verified from soap-server source — use these exactly -->

From /Users/jhogendorn/ws/soap-server/src/fault.rs:
```rust
pub enum FaultCode { Sender, Receiver }

pub struct SoapFault {
    pub code: FaultCode,
    pub reason: String,
    pub detail: Option<String>,
}

impl SoapFault {
    pub fn new(code: FaultCode, reason: impl Into<String>, detail: Option<String>) -> Self;
}
```

From /Users/jhogendorn/ws/soap-server/src/wsdl/resolver.rs:
```rust
pub trait WsdlLoader: Send + Sync {
    fn load(&self, location: &str) -> Result<Vec<u8>, WsdlError>;
}
```

From /Users/jhogendorn/ws/soap-server/src/handler.rs:
```rust
#[async_trait]
pub trait SoapHandler: Send + Sync + 'static {
    async fn handle(&self, body: Bytes) -> Result<Bytes, SoapFault>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cargo.toml and crate root (INFRA-01)</name>
  <files>Cargo.toml, src/lib.rs</files>
  <behavior>
    - Cargo.toml declares `name = "onvif-server"`, `edition = "2021"`, path dep on `soap-server = { path = "../soap-server" }`
    - Library deps: `tokio = { version = "1", features = ["sync"] }`, `async-trait = "0.1"`, `thiserror = "2"`, `bytes = "1"`, `chrono = { version = "0.4", features = ["std", "clock"], default-features = false }`, `uuid = { version = "1", features = ["v4"] }`, `quick-xml = "0.39"`
    - Dev deps: `tokio = { version = "1", features = ["full", "test-util"] }`
    - Feature flags: `default = []`, `discovery = ["socket2"]`, with `socket2 = { version = "0.5", features = ["all"], optional = true }`
    - No `tokio = { features = ["full"] }` in non-dev deps (library must not force full tokio)
    - `src/lib.rs` declares: `mod error; mod constants; mod server; mod wsdl_loader; pub mod traits;`
    - `src/lib.rs` re-exports: `pub use error::OnvifError; pub use server::{OnvifServer, OnvifServerBuilder}; pub use constants::*;`
    - Stub empty modules: `src/server.rs`, `src/wsdl_loader.rs`, `src/traits/mod.rs` (all just `// TODO` comments so lib.rs compiles)
  </behavior>
  <action>
    Create `Cargo.toml` at crate root using the exact template from RESEARCH.md Standard Stack section. The soap-server path dep is `{ path = "../soap-server" }`. Do NOT use `tokio = { features = ["full"] }` in `[dependencies]` — only in `[dev-dependencies]`.

    Create `src/lib.rs` with module declarations for `error`, `constants`, `server`, `wsdl_loader`, and `pub mod traits`. Re-export the public API: `pub use error::OnvifError; pub use server::{OnvifServer, OnvifServerBuilder}; pub use constants::*;`.

    Create stub files to satisfy the module declarations so the crate compiles:
    - `src/server.rs`: empty `pub struct OnvifServer; pub struct OnvifServerBuilder;`
    - `src/wsdl_loader.rs`: empty `pub struct EmbeddedWsdlLoader;`
    - `src/traits/mod.rs`: empty file with `// Service traits — added in plan 02`
  </action>
  <verify>
    <automated>cd /Users/jhogendorn/ws/onvif-server && cargo build -p onvif-server 2>&1</automated>
  </verify>
  <done>Crate compiles. Cargo.toml has soap-server path dep. tokio in dev-deps has "full"; in lib deps only "sync". Module structure is in place.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OnvifError type and token constants (INFRA-02, INFRA-09)</name>
  <files>src/error.rs, src/constants.rs, tests/foundation.rs</files>
  <behavior>
    - `OnvifError` has three variants: `NotImplemented`, `InvalidArgument(String)`, `ActionNotSupported`
    - `OnvifError::into_soap_fault()` maps to `SoapFault`: NotImplemented → FaultCode::Receiver + reason "Action not supported" + subcode "ter:ActionNotSupported"; InvalidArgument → FaultCode::Sender + reason from message + subcode "ter:InvalidArgVal"; ActionNotSupported → FaultCode::Receiver + same as NotImplemented
    - The `detail` field in every fault must include `xmlns:ter="http://www.onvif.org/ver10/error"` at the point of use: `format!(r#"<ter:fault xmlns:ter="http://www.onvif.org/ver10/error"><ter:subcode>{subcode}</ter:subcode></ter:fault>"#)`
    - `not_implemented() -> Result<(), OnvifError>` convenience function returns `Err(OnvifError::NotImplemented)` (use `()` return inner type until real types exist from INFRA-04)
    - Token constants in `src/constants.rs`: `PROFILE_TOKEN = "profile_0"`, `VIDEO_SOURCE_TOKEN = "video_src_0"`, `PTZ_NODE_TOKEN = "ptz_node_0"`, `PTZ_CONFIG_TOKEN = "ptz_cfg_0"`, `TRANSLATION_SPACE_FOV = "http://www.onvif.org/ver10/tptz/PanTiltSpaces/TranslationSpaceFov"`
    - Test `test_not_implemented_fault_has_ter_namespace`: call `OnvifError::NotImplemented.into_soap_fault()`, assert `fault.detail.unwrap().contains("xmlns:ter=\"http://www.onvif.org/ver10/error\"")`
    - Test `test_token_constants_defined`: assert all five constants are non-empty strings; assert `TRANSLATION_SPACE_FOV` contains "TranslationSpaceFov"
  </behavior>
  <action>
    Create `src/error.rs` with `#[derive(Debug, thiserror::Error)]` on `OnvifError`. Import `soap_server::fault::{FaultCode, SoapFault}`. Implement `into_soap_fault(self) -> SoapFault` using the pattern from RESEARCH.md Pattern 1. The `xmlns:ter` declaration MUST be inside the `detail` string — soap-server's envelope does not inject it. Also create `pub fn not_implemented() -> Result<(), OnvifError>`.

    Create `src/constants.rs` with exactly five `pub const` strings as specified. Add doc comments explaining each token's role.

    Create `tests/foundation.rs` with the two test functions above plus placeholder stubs for `test_embedded_wsdl_loader`, `test_not_implemented_returns_error`, `test_builder_accepts_service_calls`, `test_auth_bypass_includes_get_system_date_and_time` — mark the stubs `#[ignore]` with comment "implemented in later plans" so the test file compiles but only the two complete tests run.
  </action>
  <verify>
    <automated>cd /Users/jhogendorn/ws/onvif-server && cargo test -p onvif-server -- test_not_implemented_fault_has_ter_namespace test_token_constants_defined 2>&1</automated>
  </verify>
  <done>Both tests pass. `OnvifError::NotImplemented.into_soap_fault().detail` contains `xmlns:ter="http://www.onvif.org/ver10/error"`. All five token constants are non-empty and TRANSLATION_SPACE_FOV contains "TranslationSpaceFov".</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `cargo build -p onvif-server` succeeds with no errors
- `cargo test -p onvif-server -- test_not_implemented_fault_has_ter_namespace test_token_constants_defined` passes
- `cargo tree -p onvif-server` shows tokio 1.x in dependencies, NOT two separate tokio versions
</verification>

<success_criteria>
1. `cargo build -p onvif-server` succeeds on a clean checkout
2. `OnvifError::NotImplemented.into_soap_fault()` detail field contains `xmlns:ter="http://www.onvif.org/ver10/error"`
3. All five token constants defined as `pub const &str`, accessible via `onvif_server::PROFILE_TOKEN` etc.
4. No `tokio = { features = ["full"] }` in library `[dependencies]`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` using the summary template at `@/Users/jhogendorn/.claude/get-shit-done/templates/summary.md`
</output>
