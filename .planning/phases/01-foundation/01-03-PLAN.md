---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/server.rs
  - src/lib.rs
  - tests/foundation.rs
autonomous: true
requirements:
  - INFRA-06
  - INFRA-07
  - INFRA-08

must_haves:
  truths:
    - "`OnvifServer::builder().port(8080).auth(\"admin\", \"secret\").build()` compiles and returns `Ok(OnvifServer)`"
    - "`OnvifServerBuilder` accepts `.device_service(impl DeviceService)` and `.ptz_service(impl PTZService)` calls without errors"
    - "Auth bypass set in builder includes `\"GetSystemDateAndTime\"` by default (registered automatically, not by caller)"
    - "`cargo test -p onvif-server` is fully green with no ignored tests failing and no compilation errors"
  artifacts:
    - path: "src/server.rs"
      provides: "OnvifServer + OnvifServerBuilder with service registration, auth config, port config, auto auth-bypass"
      exports: ["OnvifServer", "OnvifServerBuilder"]
      contains: "GetSystemDateAndTime"
    - path: "tests/foundation.rs"
      provides: "Full test suite covering INFRA-02, INFRA-03, INFRA-05, INFRA-06, INFRA-08, INFRA-09 — all enabled, all green"
  key_links:
    - from: "src/server.rs"
      to: "crate::traits::DeviceService"
      via: "Option<Arc<dyn DeviceService>> field in OnvifServerBuilder"
      pattern: "Arc<dyn DeviceService>"
    - from: "src/server.rs"
      to: "soap_server::ServerBuilder"
      via: "Phase 2 will call soap_server::ServerBuilder inside build() — skeleton stores the fields needed"
      pattern: "auth_bypass"
---

<objective>
Complete the builder skeleton and auth wiring that make `OnvifServer::builder()` a functional API surface. The builder accepts service registrations, auth credentials, port configuration, and automatically registers the GetSystemDateAndTime auth bypass. Bring all test stubs from foundation.rs to green.

Purpose: Phase 2 will immediately extend this builder to actually start a server. Phase 1's job is to ensure all the pieces compile correctly together — service trait objects stored in the builder, auth credentials stored ready for soap-server wiring, and the auto-bypass for GetSystemDateAndTime enforced at the builder level.
Output: A complete, compilable builder API with all nine foundation tests passing.
</objective>

<execution_context>
@/Users/jhogendorn/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jhogendorn/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Verified from soap-server source -->

From /Users/jhogendorn/ws/soap-server/src/server.rs:
```rust
// ServerBuilder::auth — accepts a closure mapping username → Option<password>
pub fn auth<F>(mut self, f: F) -> Self
where
    F: Fn(&str) -> Option<String> + Send + Sync + 'static;

// ServerBuilder::auth_bypass — registers operation names that skip auth
pub fn auth_bypass<I, S>(mut self, ops: I) -> Self
where
    I: IntoIterator<Item = S>,
    S: Into<String>;
```

From src/traits/ (created in plan 02):
```rust
pub trait DeviceService: Send + Sync + 'static { ... }
pub trait MediaService: Send + Sync + 'static { ... }
pub trait PTZService: Send + Sync + 'static { ... }
pub trait ImagingService: Send + Sync + 'static { ... }
pub trait EventService: Send + Sync + 'static { ... }
```

From src/error.rs (plan 01):
```rust
pub enum OnvifError { NotImplemented, InvalidArgument(String), ActionNotSupported }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OnvifServer builder skeleton with service registration and auth (INFRA-06, INFRA-07, INFRA-08)</name>
  <files>src/server.rs, src/lib.rs</files>
  <behavior>
    - `OnvifServer::builder()` returns `OnvifServerBuilder` with `port: 8080` default and `GetSystemDateAndTime` pre-registered in auth bypass set
    - `OnvifServerBuilder` fields: `port: u16`, `username: Option<String>`, `password: Option<String>`, `device_service: Option<Arc<dyn DeviceService>>`, `media_service: Option<Arc<dyn MediaService>>`, `ptz_service: Option<Arc<dyn PTZService>>`, `imaging_service: Option<Arc<dyn ImagingService>>`, `event_service: Option<Arc<dyn EventService>>`, `auth_bypass: HashSet<String>`
    - `.port(u16) -> Self` sets port
    - `.auth(username: &str, password: &str) -> Self` stores credentials (Phase 2 will use these to configure soap-server's auth_fn)
    - `.device_service(impl DeviceService + 'static) -> Self` wraps in `Arc` and stores
    - `.media_service(impl MediaService + 'static) -> Self` same
    - `.ptz_service(impl PTZService + 'static) -> Self` same
    - `.imaging_service(impl ImagingService + 'static) -> Self` same
    - `.event_service(impl EventService + 'static) -> Self` same
    - `GetSystemDateAndTime` is inserted into `auth_bypass` in `OnvifServer::builder()` — callers never need to register it manually (per ONVIF spec and INFRA-08)
    - `build(self) -> Result<OnvifServer, BuildError>` returns `Ok(OnvifServer { /* phase 2 fields */ })` — skeleton only, no actual server binding yet
    - `BuildError` is a simple enum with at least one variant (`MissingRequiredService`) for completeness
    - `OnvifServer` struct stores the built configuration fields for Phase 2 to use
  </behavior>
  <action>
    Replace the stub in `src/server.rs` with the full builder implementation as described in the behavior section. Import `std::collections::HashSet`, `std::sync::Arc`, and `crate::traits::*`.

    The `build()` method at Phase 1 stage does NOT call soap-server's `ServerBuilder` — that wiring is Phase 2. It just validates and returns `Ok(OnvifServer { port: self.port, /* stored services */ })`.

    Add `#[derive(Debug, thiserror::Error)]` to `BuildError`:
    ```rust
    #[derive(Debug, thiserror::Error)]
    pub enum BuildError {
        #[error("Required service not registered: {0}")]
        MissingRequiredService(String),
    }
    ```

    Update `src/lib.rs` to also re-export `BuildError`:
    ```rust
    pub use server::{OnvifServer, OnvifServerBuilder, BuildError};
    ```

    Following RESEARCH.md Pattern 4 — the skeleton compiles and accepts calls; functional server start happens in Phase 2. Do NOT add tokio::main, axum listeners, or actual port binding in Phase 1.
  </action>
  <verify>
    <automated>cd /Users/jhogendorn/ws/onvif-server && cargo build -p onvif-server 2>&1</automated>
  </verify>
  <done>Crate compiles. OnvifServerBuilder accepts service registration calls via all five service methods. .auth() stores credentials. .port() sets port. build() returns Ok(OnvifServer). GetSystemDateAndTime is in auth_bypass by default.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enable all test stubs and make full suite green (INFRA-06, INFRA-08)</name>
  <files>tests/foundation.rs</files>
  <behavior>
    - `test_builder_accepts_service_calls`: create an inline struct implementing DeviceService, call `OnvifServer::builder().port(8080).device_service(StubDev).auth("user", "pass").build()`, assert it returns `Ok(_)`
    - `test_auth_bypass_includes_get_system_date_and_time`: build a default builder (no services), inspect the auth_bypass field (expose it as `pub` on OnvifServerBuilder or add a `pub fn auth_bypass(&self) -> &HashSet<String>` accessor), assert `auth_bypass.contains("GetSystemDateAndTime")`
    - All previously `#[ignore]`-marked test stubs are now enabled and pass
    - Final `cargo test -p onvif-server` is fully green (0 failures, 0 ignored tests that should now run)
  </behavior>
  <action>
    Remove all `#[ignore]` attributes from stubs in `tests/foundation.rs` that correspond to INFRA-06 and INFRA-08. Implement both tests as described in behavior.

    For `test_auth_bypass_includes_get_system_date_and_time`, add a public accessor to `OnvifServerBuilder` in `src/server.rs` if the `auth_bypass` field is private:
    ```rust
    pub fn auth_bypass_set(&self) -> &std::collections::HashSet<String> {
        &self.auth_bypass
    }
    ```

    Run `cargo test -p onvif-server` — ALL tests must pass. Fix any test that fails before marking this task complete.

    The final green test suite should cover:
    - test_not_implemented_fault_has_ter_namespace (INFRA-02)
    - test_token_constants_defined (INFRA-09)
    - test_embedded_wsdl_loader (INFRA-03)
    - test_not_implemented_returns_error (INFRA-05)
    - test_builder_accepts_service_calls (INFRA-06)
    - test_auth_bypass_includes_get_system_date_and_time (INFRA-08)
  </action>
  <verify>
    <automated>cd /Users/jhogendorn/ws/onvif-server && cargo test -p onvif-server 2>&1</automated>
  </verify>
  <done>All 6 tests pass. No tests are ignored. `cargo test` output shows "test result: ok. 6 passed; 0 failed; 0 ignored".</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `cargo build -p onvif-server` succeeds with no warnings
- `cargo test -p onvif-server` shows 6 passed, 0 failed, 0 ignored
- `OnvifServer::builder().port(9000).auth("admin", "pass").build()` compiles
- `GetSystemDateAndTime` is in auth_bypass without callers registering it
</verification>

<success_criteria>
1. `OnvifServer::builder()` compiles and accepts all five service registration methods plus `.port()` and `.auth()`
2. `build()` returns `Ok(OnvifServer)` — skeleton is functional
3. Auth bypass set includes `GetSystemDateAndTime` automatically
4. All six foundation tests pass — full phase test suite is green
5. Phase 1 success criteria from ROADMAP.md are satisfied:
   - `cargo build` succeeds on fresh checkout
   - `OnvifError::NotImplemented` serializes with xmlns:ter
   - EmbeddedWsdlLoader returns WSDL bytes
   - Token constants defined as pub const
   - `OnvifServer::builder()` accepts service registration calls
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md` using the summary template.

This is the final plan in Phase 1. The SUMMARY should note that the crate is now ready for Phase 2 (Device Management) and identify any constraints discovered during execution (e.g., which type strategy was chosen in plan 02, any WSDL import paths that needed special handling).
</output>
