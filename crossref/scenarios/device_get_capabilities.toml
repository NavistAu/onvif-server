name            = "device_get_capabilities"
service         = "device"
transport       = "http"
auth_mode       = "usernametoken"
operation       = "GetCapabilities"
schema_id       = "device-body"
http_method     = "POST"
expected_status = 200
outcome         = "success"
request_file    = "device_get_capabilities.request.xml"
invariants      = []
masks           = ["host_authority"]
# reference_mode downgraded from srvd_projection: legitimate device difference —
# srvd emits bare-authority XAddrs (no /onvif/... path) and a different capability set
# (see comparators/onvif-srvd/NOTES.md). NOTE: this scenario is also independently
# SutFail via finding F-5 (real product bug) — that failure remains red regardless.
# Our-side oracle + invariants + Layer-1 baseline still apply.
reference_mode  = "none"
