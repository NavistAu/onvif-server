name            = "device_get_services"
service         = "device"
transport       = "http"
auth_mode       = "usernametoken"
operation       = "GetServices"
schema_id       = "device-body"
http_method     = "POST"
expected_status = 200
outcome         = "success"
request_file    = "device_get_services.request.xml"
invariants      = []
masks           = ["host_authority"]
# reference_mode downgraded from srvd_projection: legitimate device difference —
# srvd emits bare-authority XAddrs (no /onvif/... path) and only 2 services vs our 5
# due to a confirmed upstream PTZ-flag inversion bug (see comparators/onvif-srvd/NOTES.md).
# Our-side oracle + invariants + Layer-1 baseline still apply.
reference_mode  = "none"
