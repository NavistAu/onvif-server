name            = "media_get_profiles"
service         = "media"
transport       = "http"
auth_mode       = "usernametoken"
operation       = "GetProfiles"
schema_id       = "media-body"
http_method     = "POST"
expected_status = 200
outcome         = "success"
request_file    = "media_get_profiles.request.xml"
invariants      = []
masks           = []
# reference_mode downgraded from srvd_projection: legitimate device difference —
# srvd uses different profile tokens/names, omits Multicast, and has different
# SessionTimeout and VideoSourceConfiguration values (see comparators/onvif-srvd/NOTES.md).
# Our-side oracle + invariants + Layer-1 baseline still apply.
reference_mode  = "none"
