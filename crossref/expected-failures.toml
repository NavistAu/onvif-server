# Scenarios known to fail the Layer-2 conformance run, tracked by finding id.
# CI fails on DRIFT from this set. The release gate (--release-green) additionally
# requires this file to be EMPTY (no reliance on a known-failure baseline).
# All Phase 2b findings (F-1..F-6, F-7, A-1) are resolved — baseline intentionally empty.
